---
title: camelize
description: Helper for converting strings to camelCase.
ai_summary: camelize converts strings to camelCase. Handles snake_case and kebab-case formats.
---

# camelize

`camelize` converts strings to camelCase.

```ts
import { camelize } from '@rvoh/dream'

camelize('hello_world') // 'helloWorld'
camelize('dream-and-psychic') // 'dreamAndPsychic'
```
