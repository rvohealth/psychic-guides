---
title: pascalize
description: Helper for converting strings to PascalCase.
---

# pascalize

`pascalize` converts strings to PascalCase.

```ts
import { pascalize } from '@rvoh/dream'

pascalize('hello_world') // 'HelloWorld'
pascalize('dream-and-psychic') // 'DreamAndPsychic'
```
