---
sidebar_position: 4
---

# repl

The REPL (called `console`) allows you to inspect the database through the lense of your application. The code used to bootstrap your repl can be found in `conf/repl.ts`

```ts
# start local repl
yarn console
> u = await User.all()
```
