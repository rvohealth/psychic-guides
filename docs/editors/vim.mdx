# vim

There is not currently a vim plugin, but there is a [watch cli command](/docs/cli/watch), which can be run in a separate terminal window, and will perform automatic syncing for you. We recommend you use this if you are using any non-vscode-based editor.