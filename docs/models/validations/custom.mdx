---
sidebar_position: 6
ai_summary: >-
  Custom validators use @deco.Validate() decorator for validation needs beyond
  provided validators. Throw ValidationError with field name and error messages.
  Validator methods are called before save.
---

# custom

Use custom validators for needs that are more specific than can be handled with provided validators:

```ts
export default class User extends Dream {
  public readonly get table() {
    return 'users' as const
  }

  public id: string

  @deco.Validate()
  public ensureEmailValid() {
    if (...) {
      throw new ValidationError(this.constructor.name, {
        email: ['email is not valid'],
      })
    }
  }
}
```
