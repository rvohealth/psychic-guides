---
sidebar_position: 2
ai_summary: "The contains validator ensures a field contains a specified substring or pattern before saving. Use @deco.Validates('contains', pattern) decorator."
---

# contains

Use the `contains` validator to ensure a certain format is met against your model before saving:

```ts
export default class User extends Dream {
  public get table() {
    return 'users' as const
  }

  public id: string

  @deco.Validates('contains', '@')
  public email: string
}
```
