---
sidebar_position: 1
ai_summary: "The presence validator ensures a field is not blank before saving. Use @deco.Validates('presence') decorator."
---

# presence

Use the `presence` validator when you want to guarantee a field is not blank before saving:

```ts
export default class Post extends Dream {
  public get table() {
    return 'posts' as const
  }

  public id: number

  @deco.Validates('presence')
  public content: string
}
```
