import RightSidebarItem from '@site/src/components/ui/right-sidebar-item'

# find

Find is a simple static method used to find records by a given primary key. it will either return the record if found, or else null

```ts
const user = await User.find(123)
```
