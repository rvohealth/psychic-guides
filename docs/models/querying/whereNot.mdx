import RightSidebarItem from '@site/src/components/ui/right-sidebar-item'

# whereNot

The `whereNot` statement will find all records that do not match the provided clauses

```ts
await User.whereNot({
  email: ops.ilike('burpcollaborator.net'),
}).all()
```
